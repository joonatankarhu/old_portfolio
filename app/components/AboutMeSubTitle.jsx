const AboutMeSubTitle = ({ subTitle }) => {
  return (
    <span className='block text-xl my-2 py-1 px-3 font-medium bg-black/90 text-white'>
      { `${subTitle}` }
    </span>
  )
}

export default AboutMeSubTitle